{
  "name": "cc-status-line-picker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A gallery of ready-to-use status lines for Claude Code",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/test-styles.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
