{
  "name": "cc-status-line-picker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A gallery of ready-to-use status lines for Claude Code",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/test-styles.mjs && node scripts/test-install.mjs",
    "test:styles": "node scripts/test-styles.mjs",
    "test:install": "node scripts/test-install.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0"
  }
}
